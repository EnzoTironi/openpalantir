[package]
name = "onto-sdk"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Typed client over the ontology engine (same surface as MCP)"

[dependencies]
onto = { path = "../onto" }
serde.workspace = true
serde_json.workspace = true

[lints]
workspace = true

[[bin]]
name = "onto"
path = "src/bin/onto.rs"
